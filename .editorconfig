root = true

# Encoding policy:
# - source/script/docs/config: UTF-8 (no BOM)
# - csv only: UTF-8 with BOM
[*]
charset = utf-8
end_of_line = crlf
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false

[*.csv]
charset = utf-8-bom
